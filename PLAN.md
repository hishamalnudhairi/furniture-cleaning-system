# PLAN.md — خطة التنفيذ بالترتيب

> ننفّذ **مرحلة واحدة في كل مرة**. بعد كل مرحلة: مراجعة + ملخص قبل الانتقال للتالية.
> الحالة الحالية: **لم يُنشأ مشروع Laravel بعد** (حسب طلب المستخدم).

## المرحلة 0 — التحضير (الحالية ✅)
- [x] إنشاء `CLAUDE.md`
- [x] إنشاء `REQUIREMENTS.md`
- [x] إنشاء `PLAN.md`
- [ ] مراجعة المستخدم والموافقة على الخطة.

## المرحلة 1 — تهيئة المشروع ✅ (مكتملة)
- [x] إنشاء مشروع Laravel جديد (Laravel 12).
- [x] قالب اتصال MySQL في `.env.example` (بدون بيانات حقيقية؛ `.env` المحلي على sqlite مؤقتًا حتى المرحلة 2).
- [x] تثبيت Tailwind CSS (v4 عبر Vite — جاهز افتراضيًا في Laravel 12).
- [x] إعداد دعم اللغتين (RTL/LTR): middleware `SetLocale` + ملفات `lang/ar.json` و`lang/en.json` + مسار تبديل اللغة.
- [x] صفحة بداية بسيطة (`/`) تؤكد عمل Laravel + Tailwind مع مبدّل لغة.

## المرحلة 2 — قاعدة البيانات والنماذج (Schema & Models) ✅ (مكتملة)
- [x] التحويل إلى MySQL (`furniture_cleaning_system`) عبر `.env`.
- [x] Migrations لـ 16 جدولًا (بدون جدول invoices — الفاتورة تُبنى من orders/order_items/payments/settings).
- [x] Models + العلاقات (customer→orders، request→images/order، order→items/payments/delivery، driver→tasks/payments، inventory→movements، user→logs).
- [x] Seeders: مدير + موظف، 8 خدمات افتراضية، إعدادات المشروع، 5 مواد مخزون.
- [x] تشغيل `migrate` و`db:seed` بنجاح.

## المرحلة 3 — المصادقة والأدوار (Auth & Roles) ✅ (مكتملة)
- [x] تسجيل دخول/خروج بسيط باستخدام جدول users (بدون حزم خارجية).
- [x] middleware `role` للصلاحيات (المدير يصل لكل شيء؛ staff = worker/accountant).
- [x] صفحة Login جميلة ثنائية اللغة RTL/LTR + لوحة تحكم مؤقتة بأزرار الأقسام.
- [x] حماية /dashboard، وتحويل غير المسجّل إلى /login، والمسجّل من /login إلى /dashboard.
- [x] اختبارات Feature (8 ناجحة). الحسابات التجريبية تعمل.
- صفحة العميل تبقى عامة بدون تسجيل (تُنفّذ في المرحلة 4).

## المرحلة 4 — صفحة العميل الخارجية (Public) ✅ (مكتملة)
- [x] صفحة عامة `/request-service` ثنائية اللغة RTL/LTR، Mobile First، بترويسة هوية من `business_settings`.
- [x] نموذج كامل: بيانات العميل (الولاية/المنطقة/النوع)، خدمات متعددة + خدمة أخرى، طريقة التنفيذ والموعد.
- [x] **تحديد الموقع** (زر "موقعي الحالي" + رابط Google Maps يدوي) — إلزامي حسب `require_customer_map_location` (افتراضي مُفعّل).
- [x] رفع صور اختياري (jpg/jpeg/png/webp، حد العدد/الحجم من الإعدادات).
- [x] الإرسال يُنشئ `service_request` فقط برقم `REQ-0001` وحالة `pending_review` (بدون order/invoice/payment) + رسالة تأكيد ثنائية اللغة.
- [x] حقول جديدة في الجداول + 6 اختبارات Feature ناجحة.

## المرحلة 5 — إدارة طلبات العملاء + التحويل لطلب رسمي ✅ (مكتملة — تأكيد الإغلاق 2026-06-30)
- [x] صفحة داخلية محمية `/admin/service-requests` بقائمة بطاقات + فلاتر (الكل/بانتظار/تم التواصل/مؤكد/ملغي) + بحث.
- [x] صفحة تفاصيل `/admin/service-requests/{id}` (بيانات/خدمات/موقع + زر Google Maps/صور/ملاحظات).
- [x] إجراءات: تم التواصل، إلغاء (بدون حذف)، تسجيل في `activity_logs`.
- [x] نموذج مراجعة وتحويل إلى طلب رسمي: تعديل العميل، أسعار/كميات، إضافة خدمة، خصم، دفع، حساب فوري للمتبقي.
- [x] التحويل يُنشئ/يستخدم `customer`، ينشئ `order` (ORD-0001) + `order_items` + `payment` عند الحاجة، ويربط بالطلب المبدئي (`status=confirmed`). لا فاتورة ولا ضريبة الآن.
- [x] قواعد الحماية (لا تحويل لملغي/مرتين، خدمة واحدة على الأقل، المدفوع ≤ الإجمالي) + 11 اختبار Feature ناجح.

## المرحلة 6 — سير عمل المحاسب للطلبات الرسمية ✅ (مكتملة — تأكيد الإغلاق 2026-06-30)
- [x] قائمة الطلبات الرسمية `/admin/orders` (بطاقات Mobile First) + فلاتر حالة الطلب وحالة الدفع + بحث.
- [x] تفاصيل الطلب `/admin/orders/{id}` (العميل/الموقع + Google Maps/البنود/الملخص المالي/المدفوعات السابقة).
- [x] تغيير الحالة (cleaning/ready/delivered/cancelled) مع تحذير المتبقي عند التسليم ومنع تعديل الملغي.
- [x] تسجيل دفعات إضافية + إعادة حساب paid/remaining وحالة الدفع تلقائيًا + منع تجاوز الإجمالي.
- [x] تعديل محدود (الخصم والملاحظات) مع منع total < paid، وتسجيل كل عملية في `activity_logs`.
- [x] ربط بطاقة "الطلبات" في لوحة التحكم + 12 اختبار Feature ناجح (المجموع 38). لا فاتورة ولا طباعة ولا ضريبة هنا.

## المرحلة 7 — الفواتير + QR + الطباعة ✅ (مكتملة — تأكيد الإغلاق 2026-06-30)
- [x] فاتورة A4 `/admin/orders/{id}/invoice` (هوية المحل/العميل/البنود/الإجماليات + ضريبة مضمّنة عند التفعيل) قابلة للطباعة.
- [x] إيصال حراري `/admin/orders/{id}/receipt` بعرض قابل للإعداد (افتراضي 80mm) عبر `thermal_paper_width`.
- [x] **QR Code** (SVG عبر bacon/bacon-qr-code، بلا خدمات خارجية) يفتح موقع العميل على Google Maps، مع رسالة بديلة عند غياب الموقع.
- [x] أزرار طباعة A4/حراري في صفحة تفاصيل الطلب + `window.print()` و `@media print`.
- [x] قراءة الإعدادات من `business_settings` (إضافة `tax_name`/`prices_include_tax`/`invoice_footer_ar`/`invoice_footer_en`/`invoice_paper_type`). لا جدول invoices مستقل.
- [x] دعم RTL/LTR + 7 اختبارات Feature ناجحة (المجموع 45).

## المرحلة 8 — السائقون والتوصيلات ✅ (مكتملة — تأكيد الإغلاق 2026-06-30)
- [x] إدارة السائقين `/admin/drivers` (قائمة/إنشاء/تعديل/تفاصيل) مع `payment_type` و`default_delivery_fee` والحالة.
- [x] صفحة التوصيلات `/admin/delivery-tasks` (بطاقات) + فلاتر (الحالة/السائق/التاريخ/بحث) + أزرار تم/فشل/ملغي.
- [x] إنشاء مهمة توصيل من تفاصيل الطلب الرسمي (pickup/delivery/pickup_and_delivery) مع حفظ رسوم العميل ومستحق السائق على المهمة فقط.
- [x] قواعد: لا مهمة لطلب ملغي/لسائق غير نشط؛ تعدد المهام مسموح؛ لا تعديل لإجمالي الطلب.
- [x] محاسبة السائق: مستحقات المهام المكتملة (تُستبعد الملغية/الفاشلة) + دفعات السائق مع منع تجاوز المستحق.
- [x] ربط بطاقتي "السائقون"/"التوصيلات" بلوحة التحكم + تسجيل العمليات في `activity_logs` + 14 اختبار Feature ناجح (المجموع 59).

## المرحلة 9 — المخزون البسيط ✅ (مكتملة — تأكيد الإغلاق 2026-06-30)
- [x] إدارة المخزون `/admin/inventory` (قائمة/إنشاء/تعديل/تفاصيل) + حالة تلقائية (كافٍ/منخفض/نفد) + فلاتر وبحث.
- [x] حركات عبر `inventory_movements` (in/out/adjustment) مع `movement_date` وتسجيل في `activity_logs`.
- [x] إضافة كمية وصرف كمية (مع منع الصرف أكثر من المتوفر) وتحديث الكمية الحالية.
- [x] تعديل يدوي (adjustment) **للمدير فقط** عبر `role:admin`.
- [x] التعديل العادي لا يغيّر الكمية مباشرة (تتغير عبر الحركات فقط) + ربط بطاقة "المخزون" بلوحة التحكم.
- [x] 12 اختبار Feature ناجح (المجموع 71).

## المرحلة 10 — الإعدادات الشاملة (Settings) ✅ (مكتملة — تأكيد الإغلاق 2026-06-30)
- [x] صفحة `/admin/settings` للمدير فقط (`role:admin`) بأقسام: المحل/أوقات العمل/الهوية/الضريبة/الفاتورة/صفحة العميل/السائقين/المخزون/اللغة/عام.
- [x] إضافة كل الأعمدة الناقصة إلى `business_settings` (دون جدول جديد) + رفع الشعار/البوستر مع معاينة.
- [x] ربط الإعدادات بالصفحات: `request-service` (تفعيل/موقع/صور/رسالة/هوية)، الفاتورة (ضريبة/QR/شعار/footer/اسم المحل/VAT)، الإيصال (عرض الورق)، لوحة التحكم (الهوية + إخفاء المخزون عند التعطيل)، السائقين (قيم افتراضية)، المخزون (تعطيل التعديل اليدوي).
- [x] قواعد: تعطيل صفحة الطلب يعرض رسالة، تعطيل الصور يخفي الرفع، عرض/إخفاء الضريبة و QR، عرض الورق الحراري 58/80.
- [x] حماية كاملة (admin فقط، staff→403، guest→login) + 15 اختبار Feature ناجح (المجموع 86).

## المرحلة 11 — التقارير الأساسية ولوحة المؤشرات ✅ (مكتملة — تأكيد الإغلاق 2026-06-30)
- [x] صفحة `/admin/reports` (للمدير فقط) ببطاقات ملخص حقيقية + روابط للتقارير التفصيلية.
- [x] تقرير المبيعات (`/sales`) بفلتر تاريخ + إجماليات (subtotal/discount/total/paid/due) وعدّادات حالة الدفع وجدول الطلبات.
- [x] تقرير الطلبات حسب الحالة، تقرير متبقي العملاء، تقرير السائقين (فلتر تاريخ/سائق)، تقرير المخزون الناقص.
- [x] تحديث لوحة التحكم بمؤشرات حقيقية (مبيعات/طلبات اليوم/قيد التنظيف/جاهز/متبقي العملاء/مستحقات السائقين/المخزون الناقص/بانتظار المراجعة).
- [x] قراءة فقط (لا تصدير، لا رسوم بيانية معقدة) + حماية (admin فقط، staff→403، guest→login).
- [x] 10 اختبارات Feature ناجحة (المجموع 96).

## المرحلة 12.5 — صفحة العملاء وصفحة الخدمات والأسعار ✅ (مكتملة — تأكيد الإغلاق 2026-06-30)
- [x] `/admin/customers` (قائمة + بحث + تفاصيل مع الطلبات/المدفوع/المتبقي/الموقع) و`/admin/customers/{id}`.
- [x] إضافة/تعديل عميل (الاسم/الهاتف/النوع/الولاية/المنطقة/العنوان/الإحداثيات/الرابط/الملاحظات) — للمدير فقط.
- [x] `/admin/services` (قائمة) + إضافة/تعديل خدمة (name_ar/name_en/unit/base_price/is_price_editable/is_active/notes) — للمدير فقط.
- [x] العرض متاح للموظف والمدير؛ الإضافة/التعديل للمدير (`role:admin`)؛ guest→login. ربط بطاقتي "العملاء"/"الخدمات" بلوحة التحكم.
- [x] إضافة أعمدة (`customers`: customer_type/wilaya/area، `services`: is_price_editable/notes) + 12 اختبار Feature ناجح (المجموع 108).

## المرحلة 12 — التحسين والمراجعة النهائية ✅ (مكتملة — تأكيد الإغلاق 2026-06-30)
- [x] مراجعة شاملة لكل الصفحات (العامة/الإدارية/الفاتورة/التقارير) — هوية بصرية وأزرار وبطاقات متناسقة Mobile First.
- [x] التحقق من `@csrf` في كل النماذج (21/21) و`enctype` في نماذج الرفع، ولا مفاتيح ترجمة بصيغة نقطية غير محلولة.
- [x] التحقق من الصلاحيات (guest→login، staff→403 على الإعدادات/التقارير، admin يصل للكل).
- [x] التحقق من RTL/LTR والترجمة ar/en، وإخفاء الإنجليزية من الصفحة العامة عند تعطيلها (`english_enabled`).
- [x] التحقق من `storage` link و`public/build` و`.env.example` (قالب MySQL).
- [x] الأداء: queries بسيطة في لوحة التحكم/التقارير + pagination في القوائم. الاختبارات كاملة **96 ناجحة** + build نظيف.

## المرحلة 13 — تجهيز الرفع على Hostinger ✅ (مكتملة — تأكيد الإغلاق 2026-06-30)
- [x] دليل نشر شامل بالعربية `DEPLOYMENT_HOSTINGER.md` (قاعدة البيانات/الرفع/public root/الأوامر/storage/الأمان/النسخ الاحتياطي/checklist/استكشاف الأخطاء).
- [x] قالب إنتاج آمن `.env.production.example` (production/debug=false/MySQL فارغ/timezone/filesystem=public) — بلا بيانات حقيقية.
- [x] التحقق من `.env.example` نظيف، وتوثيق أن `public/build` مُدرج في `.gitignore` وكيفية رفعه.
- [x] فحص نهائي: `php artisan test` (**108 ناجحة**) + `npm run build` ناجح + `route:list` سليم.
- [x] لم يُرفع شيء فعليًا ولا بيانات حقيقية — تجهيز وتوثيق فقط.

---
### قواعد التنفيذ
1. لا ننتقل لمرحلة جديدة قبل إنهاء الحالية ومراجعتها.
2. أي خطوة تثبّت حزمًا أو تشغّل migrations تحتاج **تأكيدًا صريحًا**.
3. بعد كل مرحلة: ملخص قصير + تحديث حالة هذا الملف.
